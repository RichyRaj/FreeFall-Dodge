(function (_window) {
    var w = _window,

    // Player Class 
    // Use this to create hero and the enemies
    Player = function () {
        var t = this;

        // Public API
    };

    // prototype methods go here
    // Player.prototype

})(this);